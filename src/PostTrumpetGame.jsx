import React from "react";

export default function PostTrumpetGame() {
  return (
    <div style={{
      minHeight: 120,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: '100%',
      padding: '4rem 2rem',
      background: 'white',
      borderRadius: 12,
    }}>
      <span style={{fontSize: 22, color: '#888'}}>Game Over! (placeholder)</span>
    </div>
  );
}
